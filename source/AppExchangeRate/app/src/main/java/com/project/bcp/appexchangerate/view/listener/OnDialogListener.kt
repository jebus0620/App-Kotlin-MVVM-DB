package com.project.jebus.appdefreesa.view.listener

import androidx.appcompat.app.AlertDialog

interface OnDialogListener {
    fun onClickAccept(dialog: AlertDialog?)
    interface OnDialogEventClickListener
}